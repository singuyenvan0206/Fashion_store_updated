<Window x:Class="WpfApp1.SupplierManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản Lý Nhà Cung Cấp" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#1CB5E0" Padding="20">
            <TextBlock Text="🏭 Quản Lý Nhà Cung Cấp" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Input Form -->
            <Border Grid.Column="0" CornerRadius="12" Padding="20" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,20,0">
                <StackPanel>
                    <TextBlock Text="Tên Nhà Cung Cấp *" FontWeight="Bold"/>
                    <TextBox x:Name="NameTextBox"/>

                    <TextBlock Text="Người Liên Hệ"/>
                    <TextBox x:Name="ContactTextBox"/>

                    <TextBlock Text="Số Điện Thoại"/>
                    <TextBox x:Name="PhoneTextBox"/>

                    <TextBlock Text="Email"/>
                    <TextBox x:Name="EmailTextBox"/>

                    <TextBlock Text="Địa Chỉ"/>
                    <TextBox x:Name="AddressTextBox" TextWrapping="Wrap" Height="60"/>

                    <TextBlock Text="Ghi Chú"/>
                    <TextBox x:Name="NoteTextBox" TextWrapping="Wrap" Height="60"/>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                        <Button Content="➕ Thêm" Width="80" Background="#4CAF50" Foreground="White" Click="AddButton_Click" Margin="0,0,10,0"/>
                        <Button Content="✏️ Sửa" Width="80" Background="#2196F3" Foreground="White" Click="UpdateButton_Click" Margin="0,0,10,0"/>
                        <Button Content="🗑️ Xóa" Width="80" Background="#F44336" Foreground="White" Click="DeleteButton_Click"/>
                    </StackPanel>
                    <Button Content="🧹 Xóa Form" Background="#9E9E9E" Foreground="White" Margin="0,10,0,0" Click="ClearButton_Click"/>
                </StackPanel>
            </Border>

            <!-- List -->
            <Border Grid.Column="1" CornerRadius="12" Padding="20" Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Danh Sách Nhà Cung Cấp" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <!-- Supplier Pagination Controls -->
                    <Border DockPanel.Dock="Bottom" Background="#F8F9FA" Padding="15" Margin="0,10,0,0" CornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Page Info -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="📄 Trang:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                                <TextBlock x:Name="SupplierPageInfoTextBlock" Text="1 / 1" FontWeight="Bold" Foreground="#1CB5E0"/>
                                <TextBlock Text=" • " Margin="10,0"/>
                                <TextBlock Text="🏭 Tổng:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                                <TextBlock x:Name="TotalSuppliersTextBlock" Text="0 nhà cung cấp" FontWeight="Bold" Foreground="#FF9800"/>
                            </StackPanel>
                            
                            <!-- Page Navigation -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="SupplierFirstPageButton" Content="⏮️" Width="35" Height="30" 
                                        Background="#2196F3" Foreground="White" FontWeight="Bold"
                                        Click="SupplierFirstPageButton_Click" Margin="0,0,5,0"/>
                                <Button x:Name="SupplierPrevPageButton" Content="◀️" Width="35" Height="30" 
                                        Background="#2196F3" Foreground="White" FontWeight="Bold"
                                        Click="SupplierPrevPageButton_Click" Margin="0,0,5,0"/>
                                <TextBox x:Name="SupplierCurrentPageTextBox" Width="50" Height="30" 
                                         TextAlignment="Center" VerticalContentAlignment="Center"
                                         FontWeight="Bold" Margin="0,0,5,0"
                                         KeyDown="SupplierCurrentPageTextBox_KeyDown"/>
                                <Button x:Name="SupplierNextPageButton" Content="▶️" Width="35" Height="30" 
                                        Background="#2196F3" Foreground="White" FontWeight="Bold"
                                        Click="SupplierNextPageButton_Click" Margin="0,0,5,0"/>
                                <Button x:Name="SupplierLastPageButton" Content="⏭️" Width="35" Height="30" 
                                        Background="#2196F3" Foreground="White" FontWeight="Bold"
                                        Click="SupplierLastPageButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ListBox x:Name="SuppliersListBox" SelectionChanged="SuppliersListBox_SelectionChanged" BorderBrush="#CCCCCC" BorderThickness="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="2">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="15"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="📞 " FontSize="12" Foreground="#555"/>
                                        <TextBlock Text="{Binding Phone}" FontSize="12" Margin="0,0,15,0" Foreground="#333"/>
                                        <TextBlock Text="👤 " FontSize="12" Foreground="#555"/>
                                        <TextBlock Text="{Binding ContactName}" FontSize="12" Foreground="#333"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                        <Setter Property="BorderBrush" Value="#1CB5E0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
